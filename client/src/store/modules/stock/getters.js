export default {
    currentTicker(state) {
        return state.ticker;
    },
    currentName(state) {
        return state.name;
    },
    currentDescription(state) {
        return state.description;
    },
    currentExchange(state) {
      return state.exchange;
    },
    currentSector(state) {
        return state.sector;
    },
    currentIndustry(state) {
        return state.industry;
    }

}