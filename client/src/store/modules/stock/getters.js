export default {
    currentTicker(state) {
        return state.ticker;
    },
    currentName(state) {
        return state.name;
    },
    currentDescription(state) {
        return state.description;
    }
}